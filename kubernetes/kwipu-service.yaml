apiVersion: v1
kind: Service
metadata:
  name: kwipu
  namespace: poc-kwipu
  labels:
    app: kwipu
    autopoc.io/project: kwipu
spec:
  type: ClusterIP
  selector:
    app: kwipu
  ports:
    - port: 8080
      targetPort: 8080
